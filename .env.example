# Copy this file to .env and adjust values as needed
# MongoDB connection string (leave unset to use local JSON storage)
MONGO_URL=mongodb://localhost:27017
# Override the default database and collection names if desired
MONGO_DB=explore_rpg
MONGO_COLLECTION=profiles
# Uncomment to change the auth server port
# PORT=8180
